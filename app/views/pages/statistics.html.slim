h1 Statistics

markdown:
  The first link was longified on **#{Link.first.created_at}**. Since then, a total of **#{Link.count}** links have been longified.
  
  The most popular theme is **#{Theme.most_popular.name}**, with a total of **#{Theme.most_popular.links.count}** links.
  The least popular is **#{Theme.least_popular.name}**, with a total of only **#{Theme.least_popular.links.count}**.
